# Serenade - 动静结合的个人博客系统

**Serenade**（小夜曲）—— 源自意大利语 "serenata"，意为在夜晚吟唱的歌谣。这个名字承载着优雅、宁静与诗意的美好寓意，正如这个博客承载着技术与思考的结晶，每一篇文章都是在深夜里精心谱写的乐章。

在这里，技术不再是冰冷的代码，而是有温度的诗篇；思考不再是零散的片段，而是连贯的旋律。愿每一篇文字都能如小夜曲般，为读者带来愉悦与启发。

## ✨ 核心特性

- 📝 **文章系统** - 使用 Hexo 风格 的 Markdown 格式，可以快速迁移
- 📚 **专栏功能** - 使用 Vuepress 风格组织文章，支持章节结构
- 🏷️ **标签分类** - 多维度标签管理，快速筛选文章
- 🔍 **搜索功能** - 全文搜索，支持关键词高亮
- 🌙 **暗黑模式** - 护眼的深色/浅色主题切换
- 💫 **数学公式** - 集成 KaTeX，支持 LaTeX 数学公式渲染
- 🔗 **友链展示** - 精美的友链卡片展示
- 📱 **响应式设计** - 完美适配桌面端和移动端
- ⚡ **高性能** - 基于 Nuxt 4 的服务端渲染与静态生成
- 💨 **现代化 UI** - 使用 TailwindCSS 构建精美界面

## 🛠️ 技术栈

- **框架**: [Nuxt 4](https://nuxt.com/)
- **前端**: Vue 3 + Composition API
- **样式**: [TailwindCSS](https://tailwindcss.com/)
- **Markdown 渲染**: [marked](https://marked.js.org/)
- **数学公式**: [KaTeX](https://katex.org/)
- **日期处理**: date-fns / dayjs
- **构建工具**: Vite
- **部署**: 支持静态生成和服务端部署

## 📦 项目结构

```
serenade/
├── assets/              # 静态资源
│   └── css/            # 样式文件
├── components/         # Vue 组件
│   ├── columns/        # 专栏相关组件
│   ├── friends/        # 友链组件
│   └── post/           # 文章相关组件
├── composables/        # Vue 组合式 API
│   ├── useApiFetch.js  # API 调用封装
│   ├── useDarkMode.js  # 暗黑模式
│   ├── useGroupBy.js   # 数据分组
│   ├── useSearch.js    # 搜索功能
│   └── useSearchApi.js # 搜索 API
├── content/            # 文章内容 (Markdown)
│   ├── columns/        # 专栏文章
│   └── posts/          # 普通文章
├── layouts/            # Nuxt 布局
├── pages/              # 页面路由
│   ├── columns/        # 专栏页面
│   ├── posts/          # 文章页面
│   └── tags/           # 标签页面
├── server/             # 服务端 API
│   └── api/            # API 接口
├── nuxt.config.ts      # Nuxt 配置
└── package.json        # 项目依赖
```

## 🚀 快速开始

### 安装依赖

```bash
# 使用 npm
npm install

# 或使用 yarn
yarn install

# 或使用 pnpm
pnpm install
```

### 开发模式

```bash
npm run dev
```

在浏览器中打开 [http://localhost:3000](http://localhost:3000)

### 构建生产版本

```bash
npm run build
```

### 预览生产版本

```bash
npm run preview
```

### 生成静态站点

```bash
npm run generate
```

## 📝 文章编写指南

### 1. 普通文章

在 `content/posts/` 目录下创建 Markdown 文件：

```markdown
---
title: 文章标题
date: 2024-01-01
tags: ["前端", "Vue"]
description: 文章描述
---

# 文章内容

这里是文章正文...
```

### 2. 专栏文章

在 `content/columns/<专栏名>/` 目录下创建 Markdown 文件：

```markdown
# 章节内容

这里是章节正文...
```

### 3. 数学公式支持

使用 `$...$` 或 `$$...$$` 包裹 LaTeX 公式：

```latex
# 行内公式
这是一行文字，包含 $E = mc^2$ 公式。

# 区块公式
$$
\int_{-\infty}^{\infty} e^{-x^2} dx = \sqrt{\pi}
$$
```

## 🎨 自定义配置

### 站点信息

编辑 `site.config` 文件来自定义站点标题、描述、作者等信息。

### 样式定制

- 修改 `assets/css/main.css` 调整全局样式
- 在 `nuxt.config.ts` 中配置 TailwindCSS
- 支持暗黑模式自动切换

### API 配置

`server/api/` 目录下包含所有服务端接口：

- `posts.get.js` - 文章列表
- `posts/[path].get.js` - 单篇文章
- `posts/search.get.js` - 搜索接口
- `columns.get.js` - 专栏列表
- `friends.js` - 友链数据
- `tags.get.js` - 标签列表

## 📖 使用 Composables

项目提供多个可复用的组合式 API：

### useApiFetch

```javascript
const { data, loading, error, fetchData } = useApiFetch("/api/posts", {
  page: 1,
  pageSize: 10,
  onSuccess: (result) => console.log("Success", result),
});
```

### useSearch

```javascript
const { searchKeyword, showSearch, hideSearch } = useSearch();
```

### useDarkMode

```javascript
const { isDark, toggleDarkMode } = useDarkMode();
```

---

**Star ⭐ 如果这个项目对你有帮助，请给我一个星标！**
