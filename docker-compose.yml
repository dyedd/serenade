services:
  serenade:
    image: ghcr.io/dyedd/serenade:latest
    container_name: serenade
    restart: unless-stopped
    ports:
      - "${SERENADE_PORT:-3000}:3000"
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3000
    volumes:
      - ./content:/app/content
